import 'package:flutter/material.dart';

class OrdersByUser extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Icon(
        Icons.people,
        size: 60,
      ),
    );
  }
}
